//C Program to Check Number is Positive Using IF Statement 

#include <stdio.h>
int main()
{
    int n;
    printf("Enter a Number:");
    scanf("%d", &n);
    if(n>0)
    printf("Positive Number");
    return 0;
}
